A mildly low effort dotfiles thingy,

slightly customized niri(dms), lazyvim(with additional plugins)

a default yaml for rime

a filter for easyeffects. 

a script that should hopefully installs packages correctly.


NOTE: for myself:
  figure out how to make the fresh setup easier, perhaps a script to setup the bare git repo
